const myLink = $CONTENT.combinatorics.basics.bruteForce;

export default (
    <>
        <P>
            I really don't <Ref to={myLink}>think this</Ref> is a good idea,
            bro!
        </P>
    </>
);
