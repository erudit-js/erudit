/**
 * **Caution:** images should be placed locally somewhere within current Erudit project folder
 * (preferable somewhere near the document using them).
 *
 * @title Video
 * @layout block
 * @example
 * ```tsx
 * // import vectorProjection from './assets/vector-projection.mp4';
 * <Video src={vectorProjection} width="400px" invert="dark" />
 *
 * // import complexDiagram from './assets/complex-diagram.mp4';
 * <Video src={complexDiagram} autoplay>
 *   <Caption>Complex diagram example</Caption>
 * </Video>
 * ```
 */
export const Video = '_tag_';
